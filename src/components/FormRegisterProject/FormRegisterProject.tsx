import { Box } from '@mantine/core';
import { SessionUser } from 'lib/session';

export default function FormRegisterProject({ user }: { user: SessionUser }) {
  return (
    <Box>
      <div></div>
    </Box>
  );
}
